# =============================================================================
# Playground Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# All variables prefixed with VITE_ are exposed to the browser

# -----------------------------------------------------------------------------
# Alchemy API Configuration
# -----------------------------------------------------------------------------
# Alchemy provides blockchain data APIs for querying on-chain data
# Sign up and get your API key at: https://www.alchemy.com/
# Required for: Token balance queries, transaction data, and blockchain interaction
VITE_ALCHEMY_API_KEY=your_alchemy_api_key_here

# -----------------------------------------------------------------------------
# GraphQL Indexer Configuration
# -----------------------------------------------------------------------------
# Envio GraphQL endpoint for querying indexed blockchain data
# Used for: Unwrap queue tracking and historical event queries
# Example: https://indexer.dev.hyperindex.xyz/YOUR_PROJECT_ID/v1/graphql
# Note: Queue features will not work without this configuration
VITE_ENVIO_GRAPHQL_URL=